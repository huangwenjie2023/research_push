# Knowledge

这是日常使用的 Obsidian 区域。建议把 `.system` 在 Obsidian 设置里隐藏，平时只围绕这里阅读、写笔记和写材料。

## 工作流

1. 从 `Daily/` 看每日推送。
2. 对值得读的条目，在对应 `Topics/<topic>/Papers/` 建精读笔记。
3. 把实验想法写到 `Topics/<topic>/Ideas/` 或 `Experiments/`。
4. 每周把碎片整理到 `Synthesis/`。
5. 写材料时放到 `Writing/`，并把新的关注点写回 `Feedback/`。

